! All Fortran programs start with the keyword program and end with the keyword end program, 
! followed by the name of the program.

program helloworld
    ! The implicit none statement allows the compiler to check that all your variable types are declared properly.
    ! You must always use implicit none at the start of every program.

    ! This simple program adds two numbers
    implicit none

    ! hello world
    print *, 'Hello world!'

end program helloworld